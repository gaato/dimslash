{
  "name": "dimslash-dev",
  "image": "nimlang/choosenim:latest",
  "customizations": {
    "vscode": {
      "extensions": [
        "nimlang.nimlang"
      ]
    }
  },
  "postCreateCommand": "bash -lc 'if command -v nim >/dev/null 2>&1; then nim --version; else echo \"Nim is not installed in this image. Install Nim 2.x before building.\"; fi'"
}
